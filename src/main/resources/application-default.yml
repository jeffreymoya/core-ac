# Development properties
server:
  port: ${ACCESS_CONTROL_SERVER_PORT}

logging:
  level:
    org:
      apache:
        kafka:
          clients:
            NetworkClient: ERROR

#  kafka:
#    bootstrap-servers: localhost:9092
#    producer:
#      properties:
#        acks: all
#    consumer:
#      group-id: main
#      auto-offset-reset: earliest
#      default-topic: general





